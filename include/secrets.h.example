#pragma once

// Copy to include/secrets.h and fill in. Gitignored.
// WiFi is the only local config; none of the three APIs need a key.
#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
